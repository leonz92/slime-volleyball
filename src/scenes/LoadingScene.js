import { Scene } from 'phaser';

export default class LoadingScene extends Scene {
  constructor () {
    super('LoadingScene');
  }
  create () {
    console.log('made it booyah slime slatslatslat$$!')
  }
}
